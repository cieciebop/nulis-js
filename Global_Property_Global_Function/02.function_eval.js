/*
Function eval() digunakan untuk memproses string menjadi perintah JavaScript.
 Fungsi ini membuat string menjadi bagian dari kode program

*/

var foo = "100 + 30";
console.log(foo);

var bar = eval(foo);
console.log(bar);
